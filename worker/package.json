{
  "name": "teleblog-auth-worker",
  "version": "1.0.0",
  "description": "Authentication worker for TeleBlog",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}